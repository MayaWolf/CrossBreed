<Button
	x:Class="CrossBreed.Windows.Views.IconButton"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:CrossBreed.Windows.Views"
	mc:Ignorable="d"
	x:Name="this"
	d:DesignHeight="300" d:DesignWidth="300" Cursor="Hand">
	<Button.Template>
		<ControlTemplate TargetType="{x:Type local:IconButton}">
			<Border Background="Transparent">
				<Path Data="{TemplateBinding Geometry}" Stroke="{DynamicResource Theme.Foreground}" Fill="{DynamicResource Theme.Foreground}"
					StrokeThickness="{TemplateBinding StrokeThickness}" />
			</Border>
		</ControlTemplate>
	</Button.Template>
</Button>